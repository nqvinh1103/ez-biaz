namespace EzBias.Infrastructure.Seeding;

public static class DataSeeder
{
    public static Task SeedAsync(IServiceProvider services)
    {
        // TODO: read frontend/src/mock/mockData.js (or exported JSON) and seed stable ids (u1, p1, a1, o1)
        return Task.CompletedTask;
    }
}
