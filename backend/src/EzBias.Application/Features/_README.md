# Features

This folder groups use-cases by feature/entity (Option B):
- Application contains the **use-case services** (business logic)
- Infrastructure contains **repositories + external implementations** (EF Core, Auth providers, etc.)
